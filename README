The OpenVR SDK is now available here: https://github.com/ValveSoftware/openvr

This repository contained an old version of the SteamVR runtime. It has been
deleted to avoid confusion with the new SDK. If you need the old runtime code,
it is still available in the history.

